# ===== Conozca Monorepo - Environment Variables Example =====
# Copiar este archivo a .env y ajustar los valores

# ===== Application =====
NODE_ENV=development
API_PORT=3000

# ===== Database =====
POSTGRES_USER=conozca
POSTGRES_PASSWORD=conozca_dev_password_change_in_production
POSTGRES_DB=conozca_db
POSTGRES_PORT=5432

# Database URL (usado por Prisma)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public

# ===== JWT Secrets =====
# IMPORTANTE: Cambiar en producción con valores seguros
# Generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=dev_secret_change_in_production_use_64_char_random_string
JWT_REFRESH_SECRET=dev_refresh_secret_change_in_production_use_64_char_random_string

# ===== Throttling / Rate Limiting =====
THROTTLE_TTL=600000      # 10 minutos en ms
THROTTLE_LIMIT=100       # 100 requests por ventana

# ===== CORS =====
# Dominios permitidos separados por coma
CORS_ORIGIN=http://localhost:3001,http://localhost:3002,http://localhost:5173

# ===== pgAdmin (opcional) =====
PGADMIN_EMAIL=admin@conozca.org
PGADMIN_PASSWORD=admin_change_in_production
PGADMIN_PORT=5050

# ===== Email Service (futuro) =====
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=noreply@conozca.org

# ===== Cloud Storage (futuro) =====
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
# S3_BUCKET=conozca-uploads

# ===== Analytics (futuro) =====
# GOOGLE_ANALYTICS_ID=
# SENTRY_DSN=

# ===== Feature Flags =====
ENABLE_SWAGGER=true
ENABLE_AUDIT_LOG=true
ENABLE_RATE_LIMITING=true
